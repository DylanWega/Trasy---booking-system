package com.example.trasy.data;

import retrofit2.http.GET;
import Model.Flight;
public interface ApiInterface {
/*
    @GET("https://skyscanner44.p.rapidapi.com/fly-to-country?"
            + "destination=" + arrival
            + "&origin=" + departure
            + "&departureDate=" + departureDate
            + "&returnDate=" + returningDate
            + "&currency=EUR"
            + "&locale=en-GB"
            + "&country=UK");

 */
}
